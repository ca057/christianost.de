---
import "../styles/variables.css";
---

<pre><slot /></pre>

<style>
  pre {
    margin: var(--spacing) 0;
    padding: calc(var(--spacing) / 2);
    background-color: #e9e9ed; /* TODO: use an aligned shade of gray here */
    border-radius: calc(var(--spacing) / 2);
  }
</style>
