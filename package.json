{
  "name": "christianost.de",
  "private": true,
  "scripts": {
    "start": "rm -r ./dist && npx @11ty/eleventy --serve",
    "build": "rm -r ./dist && npx @11ty/eleventy"
  },
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0-beta.1",
    "@11ty/eleventy-plugin-webc": "^0.11.2"
  },
  "devDependencies": {
    "browserslist": "^4.24.0",
    "lightningcss": "^1.27.0"
  }
}
