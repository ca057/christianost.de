{
  "name": "christianost.de",
  "version": "1.0.0",
  "description": "Code for my awesome online portfolio",
  "main": "",
  "repository": "git@github.com:ca057/christianost.de.git",
  "author": "Christian Ost <christian@christianost.de>",
  "license": "BSD3",
  "private": true,
  "scripts": {
    "lint:js": "eslint .",
    "lint:css": "stylelint src/**/*.css",
    "lint": "npm run lint:js && npm run lint:css",
    "test": "jest",
    "start": "parcel src/index.html",
    "dev:server": "nodemon server/index.js",
    "prebuild": "npm run clean && npm run lint && npm run test",
    "build": "npm run build:web && npm run build:server",
    "build:web": "parcel build src/index.html -d dist/web",
    "build:server": "ncc build server/index.js -o dist/server",
    "clean": "rm -rf dist coverage"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.12.1",
    "eslint": "^4.15.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-import": "^2.8.0",
    "jest": "^24.0.0",
    "nodemon": "^1.18.9",
    "parcel-bundler": "^1.11.0",
    "postcss-preset-env": "^6.5.0",
    "posthtml": "^0.11.3",
    "posthtml-include": "^1.2.0",
    "prettier": "^1.16.1",
    "stylelint": "^8.4.0",
    "stylelint-config-recommended": "^2.0.1"
  },
  "dependencies": {
    "dotenv": "^6.2.0",
    "got": "^9.6.0",
    "normalize.css": "^8.0.1"
  }
}
