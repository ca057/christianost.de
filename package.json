{
  "name": "christianost.de",
  "private": true,
  "scripts": {
    "start": "rm -rf ./dist && npx @11ty/eleventy --serve",
    "build": "rm -rf ./dist && npx @11ty/eleventy"
  },
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-webc": "^0.11.2",
    "fast-blurhash": "^1.1.4"
  },
  "devDependencies": {
    "browserslist": "^4.24.0",
    "lightningcss": "^1.27.0",
    "temporal-polyfill": "^0.2.5"
  }
}
